[core]
	editor = vim
	autocrlf = input
[credential]
	helper = cache --timeout=3600
[http]
	sslVerify = false
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[usethis]
[safe]
	directory = ~/jkl
